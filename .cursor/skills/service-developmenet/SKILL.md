---
name: service-development
description: Use this skill whenever working on an Angular service. Enforces single-concern scope, no `standalone: true`, and asking before adding `providedIn: 'root'`.
---
# Service Development Skill

# Prework Validation

You are to **ALLWAY** do the following before performing any work:
- **ALWAYS** ask before adding `providedId: 'root'`.

# Rules
- Must **ONLY** focus on one logic group of functionality.
- **NEVER** use `standalone: true` in the `@Injectable` decorator.

# **REQUIRED** End Review

The following must be reviewed after finishing the task before the task can be considered complete:

- Review for any glaring issues.
