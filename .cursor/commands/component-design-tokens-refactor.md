<!-- 
Inputs:
- specific directory for the component to review
-->
You are tasked to pull values from the css in the references component and place them in `list-tokens.css` file in the same directory and then update the original css to use those variables or update them as needed based on `.claude/rules/patterns/css-local-variables.md`.
